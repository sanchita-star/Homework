let num=3;
        console.log(num);
        console.log(typeof num);
        let nym="erty";
        console.log(nym);
        console.log(typeof nym);
        let nrm=123456789;
        console.log(nrm);
        console.log(typeof nrm);
        let nwm=null;//this will show object as datatype as if it shows null then program will get stopped its execution
        console.log(nwm);
        console.log(typeof nwm);
        let nim;
        console.log(nim);
        console.log(typeof nim);

 
